#!/bin/bash -e

# Docker entrypoint for Outpost
# Runs setup-production before starting the application

# Remove stale PID file if it exists
rm -f tmp/pids/server.pid

# Run production setup (database, directories, etc.)
./bin/setup-production

# Execute the main command (usually ./bin/boot)
exec "${@}"
